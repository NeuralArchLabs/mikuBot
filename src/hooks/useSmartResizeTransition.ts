/**
 * DEPRECATED: Use directly from contexts
 * This file is kept for backwards compatibility only
 * 
 * NEW: Import from ResizeContext instead
 * import { useSmartResizeTransition } from '../contexts';
 */
export { useSmartResizeTransition } from '../contexts/ResizeContext';
