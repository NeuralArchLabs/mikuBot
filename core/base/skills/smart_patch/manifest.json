{
    "name": "smart_patch",
    "description": "Edición quirúrgica de archivos. Reemplaza un bloque de texto exacto por otro. Perfecto para modificar código específico sin afectar el resto del archivo.",
    "parameters": {
        "type": "object",
        "properties": {
            "path": {
                "type": "string",
                "description": "Ruta del archivo a modificar"
            },
            "find": {
                "type": "string",
                "description": "Bloque exacto de texto a buscar (debe coincidir exactamente, incluyendo espacios)"
            },
            "replace": {
                "type": "string",
                "description": "Texto nuevo para reemplazar"
            },
            "create_backup": {
                "type": "boolean",
                "description": "Crear backup antes de realizar el cambio",
                "default": true
            }
        },
        "required": ["path", "find", "replace"]
    },
    "runtime": "python",
    "entry": "main.py"
}