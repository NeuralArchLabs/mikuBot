{
    "name": "web_research",
    "description": "Intelligent web research tool that not only searches for snippets but also extracts and analyzes the true content of the most relevant sites to avoid superficial or inaccurate information.",
    "parameters": {
        "type": "object",
        "properties": {
            "query": {
                "type": "string",
                "description": "The specific topic or question to investigate."
            },
            "max_sites": {
                "type": "integer",
                "description": "Number of sites to read and extract (1-5). Default is 3.",
                "default": 3
            }
        },
        "required": [
            "query"
        ]
    },
    "runtime": "python",
    "entry": "main.py"
}