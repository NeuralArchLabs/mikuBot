{
    "name": "web_research",
    "description": "Intelligent web research tool that not only searches for snippets but also extracts and analyzes the true content of the most relevant sites to avoid superficial or inaccurate information.",
    "parameters": {
        "type": "object",
        "properties": {
            "query": {
                "type": "string",
                "description": "Topic to investigate."
            },
            "categories": {
                "type": "array",
                "items": { "type": "string", "enum": ["general", "images", "videos", "news", "maps", "shopping", "it", "social"] },
                "description": "SearXena categories. Default: ['general']. For tech/code, ['it', 'general'] is highly effective.",
                "default": ["general"]
            },
            "max_sites": {
                "type": "integer",
                "description": "Sites to read/extract (1-5). Default: 3.",
                "default": 3
            }
        },
        "required": [
            "query"
        ]
    },
    "runtime": "python",
    "entry": "main.py"
}